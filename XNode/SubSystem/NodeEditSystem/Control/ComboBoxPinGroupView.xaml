<local:PinGroupViewBase x:Class="XNode.SubSystem.NodeEditSystem.Control.ComboBoxPinGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XNode.SubSystem.NodeEditSystem.Control"
             mc:Ignorable="d"
             d:DesignWidth="280" Height="25">
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition x:Name="InputBoxArea" Width="120"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>

        <!-- 左引脚 -->
        <Image x:Name="Icon_LeftPin" Width="11" Height="11" IsHitTestVisible="False" Grid.Column="0"/>
        <Grid x:Name="LeftPinArea" Background="Transparent" Grid.Column="0" Width="17" Height="17"/>

        <!-- 数据名称 -->
        <TextBlock x:Name="Block_Name" Text="选项"
                   HorizontalAlignment="Left" VerticalAlignment="Center"
                   Grid.Column="1" Foreground="White" Margin="1,0,5,0"/>

        <!-- 下拉选择框 -->
        <ComboBox x:Name="ComboBox_Value"
                  Grid.Column="2"
                  VerticalAlignment="Center"
                  Height="20"
                  Padding="4,0"
                  Background="#3A3A3A"
                  Foreground="#FFFFFF"
                  BorderBrush="#606060"
                  BorderThickness="1"
                  Margin="0,0,2,0"
                  SelectionChanged="ComboBox_Value_SelectionChanged">
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="18"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- 边框和背景 -->
                                    <Border x:Name="Border"
                                            Grid.ColumnSpan="2"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="2"/>
                                    
                                    <!-- 显示内容 -->
                                    <ContentPresenter x:Name="ContentSite"
                                                    Grid.Column="0"
                                                    IsHitTestVisible="False"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                    Margin="4,0,0,0"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left"
                                                    TextElement.Foreground="#FFFFFF">
                                        <ContentPresenter.Resources>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                            </Style>
                                        </ContentPresenter.Resources>
                                    </ContentPresenter>
                                    
                                    <!-- 下拉按钮 -->
                                    <ToggleButton x:Name="ToggleButton"
                                                Grid.Column="1"
                                                Focusable="false"
                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                ClickMode="Press"
                                                Background="Transparent"
                                                BorderThickness="0">
                                        <Path x:Name="Arrow"
                                              Fill="{TemplateBinding Foreground}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z"/>
                                    </ToggleButton>
                                    
                                    <!-- 下拉弹出层 -->
                                    <Popup x:Name="Popup"
                                           Placement="Bottom"
                                           IsOpen="{TemplateBinding IsDropDownOpen}"
                                           AllowsTransparency="True"
                                           Focusable="False"
                                           PopupAnimation="Slide">
                                        <Grid x:Name="DropDown"
                                              SnapsToDevicePixels="True"
                                              MinWidth="{TemplateBinding ActualWidth}"
                                              MaxHeight="200">
                                            <Border x:Name="DropDownBorder"
                                                    Background="#2A2A2A"
                                                    BorderBrush="#606060"
                                                    BorderThickness="1"
                                                    CornerRadius="2"
                                                    Padding="1">
                                                <ScrollViewer Margin="0"
                                                            SnapsToDevicePixels="True">
                                                    <StackPanel IsItemsHost="True"
                                                              KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                </ScrollViewer>
                                            </Border>
                                        </Grid>
                                    </Popup>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#808080"/>
                                    </Trigger>
                                    <Trigger Property="IsDropDownOpen" Value="True">
                                        <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.Style>
            
            <!-- ComboBoxItem样式 -->
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Padding" Value="6,3"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter x:Name="ContentPresenter">
                                        <ContentPresenter.Resources>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                            </Style>
                                        </ContentPresenter.Resources>
                                    </ContentPresenter>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#4A7BA7"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#5A8BC7"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.ItemContainerStyle>
        </ComboBox>

        <!-- 右引脚 -->
        <Image x:Name="Icon_RightPin" Width="11" Height="11" Grid.Column="3" IsHitTestVisible="False"/>
        <Grid x:Name="RightPinArea" Background="Transparent" Grid.Column="3" Width="17" Height="17"/>
    </Grid>
</local:PinGroupViewBase>
